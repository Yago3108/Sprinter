import 'package:flutter/material.dart';

class PaginaRendimento extends StatelessWidget {
  const PaginaRendimento({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: null,
    );
  }
}